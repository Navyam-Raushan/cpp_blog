#include <iostream>
#include <cstring>
#include <string>
#include <cctype>
using namespace std;

int main()
{
    string st;
    int l, i;
    char f1[50];
    gets(f1);
    cout << f1;
    l = strlen(f1);
    for (i = 0; i <= l; i++)
    {
        if(isspace)
    }

    return 0;
}